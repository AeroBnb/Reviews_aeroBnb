  config:
    target: "http://localhost:7000"
    # target: "http://18.237.172.179"
    phases:
      - duration: 60
        arrivalRate: 120
    processor: "./autoGeneratedID.js"
    defaults:
      headers:
        x-my-service-auth: '987401838271002188298567'
  scenarios:
    - flow:
      - function: "generateRandomID"
      - get:
          # url: "/listing?id=57"
          url: "/listing?id={{id}}"
          # url: "/reviews?id=57"
          # url: "/reviews?id={{id}}"
      # - get:
          # url: "/listing?id=9999999"
          # url: "/ratings?id={{id}}"
          # url: "/ratings?id=57"

          # - post:
      #     url: "/reviews?id={{id}}"
      #     json:
      #       price: 999
      #       maxguests: 4
      #       minstay: 5